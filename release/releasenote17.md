# MSV 乐谱查看器 v2.1.9

## 修复

- **修复书签不可见与缩略图面板偏移的问题（v2.1.8 回归）**：上一版将面板容器改为 Box 图层结构时，缩略图面板未右对齐且与书签栏同位堆叠，导致书签被面板完全遮盖、整页位置偏移；现面板正确 `CenterEnd` 对齐（贴屏幕右侧），书签栏位于其下一图层（可见 220dp + 面板之下 64dp）

## 技术变更

- 书签栏宽度 284dp（可见 220 + 面板之下 64），书签条 End 对齐后右端统一延伸至面板之下，去掉多余 offset
- versionCode=17, versionName=2.1.9
