# MSV 乐谱查看器 — 功能设计总览

> MSV (Music Score Viewer) 是一款面向乐手/学生的 Android 平板与手机乐谱阅读器，
> 围绕"练琴时的真实场景"设计：免接触翻页、大屏双页对照、在线找谱即下即看。
> 本文介绍应用全部功能与设计思路；代码结构详见 [STRUCTURE.md](STRUCTURE.md)。

---

## 1. 乐谱阅读器（核心舞台）

### 1.1 翻页
- **点击三分区**：屏幕左 1/3 上一页、右 1/3 下一页、中央切换工具栏（防误触按 48px 位移窗口判定双击/单击）
- **拖拽跟手翻页**：横向拖拽页面跟手滑动，越界 1/4 阻尼回弹；反向拖动自动取消
- **横屏双页对照**：横屏下自动判断并开启双页模式，左右对页作为整体滑动；竖屏单页
- **快速翻动优化**：2 秒内连翻 ≥4 页进入快速模式（当前页优先 + 邻页低分辨率并行渲染），停止 1 秒后自动恢复全分辨率
- **渐进渲染管线**：中央页先出快速预览（0.35x）再无缝替换全尺寸（0.95 质量），邻页与预载窗口分级质量，杜绝翻页空窗

### 1.2 缩放
- **双指捏合**：以双指**起始中点为缩放焦点**（手指之间内容保持不动），支持 1x–8x，质心平移、钳制越界
- **双击**：正常模式中央双击直接放大到 2x 并进入缩放模式；缩放模式双击一步恢复原状（缩放与平移动画并行，无两段过程）
- **自动退出**：双指捏回接近原比例时无需松手，自动动画归位并退出缩放模式；双击判定对慢速双击（400ms 窗口）与手部微动（24dp 路径兜底）均友好
- 缩放模式下单指平移（带边界钳制），捏合转单指自动重锚定不跳变

### 1.3 显示
- **明暗主题**：顶栏重置键长按切换；暗色下页面对象素级反色（白底黑谱 → 黑底白谱），护眼且省墨
- **页面跳转**：点击页码输入任意页直达
- **书签系统**：PDF 原生大纲读写（颜色编码 + 标题，升序写入），侧边书签条半透明彩色飘带，长按编辑/删除，跳转精准到页
- **缩略图面板**：右侧滑出网格，快速跳页；当前页高亮并自动滚动定位
- **会话恢复**：退出应用后重进自动回到上次阅读的文件与页码（含每文件独立页码记忆）

---

## 2. 面部识别免接触翻页

- 基于 MediaPipe Face Landmarker（本地离线推理，不上传任何图像）
- **手势触发**：左/右眨眼翻上/下一页，撅嘴可设为独立触发或组合触发（左右撅嘴区分方向）
- **抗误触管线**：blendshapes 信号 EMA 平滑 + 迟滞双阈值 + 800ms 动作冷却
- **可调参数**：眨眼/撅嘴阈值、左右偏好偏置、触发模式（眨眼/撅嘴/两者）
- **镜像预览（WYSIWYG）**：送检画面即预览画面，逐行拷贝 YUV 兼容各设备 stride 填充；预览叠加关键点连线与四项手势实时分数
- 后台保持运行：面板关闭时相机管线以 1dp 隐藏视图存活，翻谱零中断

---

## 3. 谱架（本地乐谱库）

- **导入**：系统文件选择器（PDF / 图片），持久化访问权限，启动即列出
- **双列瀑布流**：卡片高度按乐谱首页**真实比例**与文件名行数自适应，自然错开无留空；缩略图白底干净显示（暗色主题自动反色）
- **管理**：重命名、删除（二次确认 + 缓存清理）、按日期/名称排序（渲染层兜底，切换即时生效）
- **即点即读**：点击卡片直接打开并恢复上次页码
- **IMSLP 在线导入入口**：谱架内一键进入在线乐谱库

---

## 4. IMSLP 在线乐谱库

### 4.1 搜索
- 应用内直连 IMSLP MediaWiki API：**作品（主命名空间全文）与作曲家分类两组结果并行查询**，分组展示
- **搜索历史**：最近 10 条持久化（跨冷启动），点击直接搜索，一键清空
- **首次使用免责声明**：弹窗强调 IMSLP 内容的版权法规（乐谱公有领域状态因国家/地区而异，需自行确认并遵守当地版权法），同意后永久生效

### 4.2 官方页浏览（内嵌 WebView）
- 点击作曲家/作品后**直接打开 IMSLP 官方页面**（真实浏览器上下文，人机验证通过率最大化），登录/验证状态随会话保留
- **加载提速**：拦截广告/统计类第三方资源（gtag/Clarity/主流广告网络），页面加载与流量显著下降；官方功能与人机验证组件不受影响
- **加载反馈**：页面加载进度条 + 半透明"加载中"覆盖层（内容渐进可见）
- **顶部收起式搜索框**：默认收起为小胶囊不占空间，点击展开（过渡动画 + 自动聚焦），提交后切回应用内结果网格，返回时回到刚才浏览的页面
- **浏览状态保持**：误触弹窗边缘关闭后重开，自动恢复到最后浏览的官方页面（直到下次冷启动）；横竖屏切换弹窗不退出

### 4.3 下载
- **拦截即下**：在官方页点击乐谱 PDF 链接自动转为应用内下载（进度条实时显示），完成后自动导入谱架并打开
- **跳过免责声明页**：预置站点同意 cookie，不再弹出确认
- **跳过 12 秒等待页**：等待页的真实下载地址从页面数据中直接解析、立即下载（倒计时为纯前端行为）
- **人工验证自动衔接**：命中 Bot Check 门禁时，验证组件在 WebView 内渲染；用户完成验证后自动检测并继续下载（含蜜罐字段守卫，防自动填充导致验证静默失败）
- **View 预览兜底**：作品页在线预览组件托管于部分网络不可达的域，点击 View 约 2 秒未渲染时自动启用自绘预览（可翻页/关闭），官方组件可用时优先官方

---

## 5. 设置与自更新

- **版本与更新日志**：显示当前版本；"查看本版本更新日志"按已安装版本从发布平台拉取完整日志
- **OTA 应用内更新**：静态清单优先（不走 API 配额）+ 双平台（Gitee/GitHub）并行兜底；应用内流式下载、**断点续传、暂停/继续**、下载完成自动引导安装；升级后自动清理历史安装包
- **开源与内容许可**：完整开源组件清单（Apache 2.0 等）+ IMSLP 内容协议（CC BY-SA 4.0）与使用说明 + Peachnote 预览接口声明
- **支持开发者**：作者的话与支持入口

---

## 6. 技术设计要点

| 维度 | 选择 | 说明 |
|---|---|---|
| UI | Jetpack Compose + Material 3 | 单 Activity、状态驱动，全部面板为滑出式组件 |
| 架构 | MVVM（ViewModel + StateFlow） | UI 状态集中管理；跨弹窗开关/旋转存活的状态提升至 ViewModel |
| PDF | Android PdfRenderer + pdfbox-android | 系统渲染器负责显示，pdfbox 负责书签读写 |
| 面部识别 | MediaPipe FaceLandmarker + CameraX | 纯本地推理，YUV 解码逐行兼容各设备 stride |
| 网络 | HttpURLConnection（零第三方网络库） | 搜索/下载/OTA 全链路自研，含断点续传与魔数校验 |
| 持久化 | DataStore Preferences | 会话、页码映射、搜索历史、免责声明确认 |
| 手势 | awaitEachGesture 原始指针处理 | 单指/双指/双击在同一管线内优先级仲裁，规避手势冲突 |
| 发布 | 双平台 OTA（GitHub + Gitee） | tag == versionName；应用内自更新，旧版即升 |

---

## 7. 隐私与版权

- 面部识别**完全本地推理**，不采集、不上传任何图像或生物特征数据
- 乐谱内容来自 IMSLP 公开接口，本应用仅作浏览与下载工具，不存储、不转售任何乐谱
- 乐谱在各国/地区的公有领域状态不同，下载与使用前请自行确认并遵守当地版权法规
